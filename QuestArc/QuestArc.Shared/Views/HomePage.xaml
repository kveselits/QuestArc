<Page
    x:Class="QuestArc.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuestArc.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" >

    <Page.Resources>
        <Flyout x:Key="flyout">
            <Flyout.FlyoutPresenterStyle>
                <Style TargetType="FlyoutPresenter">
                    <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled"/>
                    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
                    <Setter Property="IsTabStop" Value="True"/>
                    <Setter Property="TabNavigation" Value="Cycle"/>
                </Style>
            </Flyout.FlyoutPresenterStyle>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock TextWrapping="Wrap" Text="This is some text in a flyout." Grid.Row="0"/>
                <Button Content="Add New Task" Click="OnFlyOutButtonClickAsync" Grid.Row="1" HorizontalAlignment="Right"/>
                <Button Content="Close" Click="OnFlyoutCloseButtonClick" Grid.Row="1" HorizontalAlignment="Left"/>
            </Grid>
        </Flyout>
    </Page.Resources>

    <Grid>
        <CalendarView x:Name="calendarView" 
            CalendarViewDayItemChanging="CalendarView_OnCalendarViewDayItemChanging" 
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            
        </CalendarView>
    </Grid>
</Page>
